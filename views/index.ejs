<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nodemon-07</title>
    <script src="https://cdn.tailwindcss.com"></script>

</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900 p-10 text-white">

        <div class="text-center justify-center">
            <div class="flex justify-center items-center h-full">
                <h1 class="bg-pink-600 py-2 px-5 mb-4 font-bold text-2xl rounded max-w-max">Todo App</h1>
            </div>
        </div>
        <div class="form "></div>
        <form action="/create"  method="post">
            <input class="block px-5 w-full rounded-lg py-2 bg-zinc-800  outline-none" type="text" placeholder="Title goes here.." name="title" >

            <textarea class="block px-5 w-full rounded-lg py-2 bg-zinc-800 resize-none mt-2 text-white outline-none" placeholder="Write your text here"  name="details"></textarea>

            <input class="mt-2 text-white bg-green-700 font-bold px-3 py-2 rounded" type="submit" value="Create Task">
           
        </form>
        <div class="tasks pt-10 flex gap-6 flex-wrap">

      <% if(files.length >0) { %>
       <% files.forEach(function(val){ %>
    
        <div class="task  w-72 px-3 py-4 bg-zinc-800 rounded-md">
            <h2 class="text-white text-2xl tracking-tighter"><%= val %></h2> 
                                                                            <!-- har baar alag ke liye val -->
            <a class="text-blue-500 inline-block mt-3" href="/file/<%= val %>">Read More</a>
        
         <!-- 🔴 Delete Button -->

         <div class=" flex justify-between items-center
         ">
  <form action="/delete" method="POST" class="mt-2 ">
    <input type="hidden" name="filename" value="<%= val %>">
    <input 
      type="submit" 
      value="Delete" 
      class="text-red-500 bg-red-600 px-3 py-1 rounded border border-red-500 cursor-pointer text-white"
    >
    
  </form>

  <a class="text-white font-bold bg-yellow-600 py-1 px-5 inline-block rounded-md mt-3" href="/edit/<%= val %>">Edit</a>
</div>
        
        
        
        </div>
    
         <% }) %>
    <% } else {  %>
    <h3 class="text-zinc-600"> No task yet</h3>
    <% } %>
        
          </div>
    </div>
   
</body>
</html>